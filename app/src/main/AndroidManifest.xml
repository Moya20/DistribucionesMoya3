<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.distribucionesmoya">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.Carrito"
            android:label="@string/title_activity_carrito"></activity>
        <activity
            android:name=".view.ProductosActivity"
            android:label="Productos" />
        <activity
            android:name=".view.Register"
            android:label="Distribuciones Moya">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.MainActivity"
            android:label="Categorias de Productos"  />
        <activity android:name=".view.ForgotPass"
            android:label="Recupera contraseña" />
        <activity android:name=".view.Login"
            android:label="Iniciar sesion"/>
    </application>

</manifest>